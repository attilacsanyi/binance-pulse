# ui

This library was generated with [Nx](https://nx.dev).

```bash
pnpm nx g @nx/angular:library --directory=libs/ui --name=ui --changeDetection=OnPush --importPath=@bp/ui --inlineStyle=true --inlineTemplate=true --prefix=bp --tags=ui
```

## Running unit tests

Run `nx test ui` to execute the unit tests.
